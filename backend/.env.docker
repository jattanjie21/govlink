# Docker Compose environment template (committed AS-IS).
#
# These are dev defaults. For production deployments, override at least
# POSTGRES_PASSWORD via your orchestrator's secrets system; do NOT commit
# real production secrets to git.

POSTGRES_PASSWORD=govlink_dev
DB_PORT=5432
API_PORT=8000
RATE_LIMIT=60
CORS_ORIGINS=*
LOG_LEVEL=INFO
